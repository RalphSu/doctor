<h1>Listing work_logs</h1>

<table>
  <tr>
    <th>Doctor</th>
    <th>Hosipital</th>
    <th>Title</th>
    <th>Certificate</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @work_logs.each do |work_log| %>
  <tr>
    <td><%= work_log.doctor %></td>
    <td><%= work_log.hosipital %></td>
    <td><%= work_log.title %></td>
    <td><%= work_log.certificate %></td>
    <td><%= link_to 'Show', work_log %></td>
    <td><%= link_to 'Edit', edit_work_log_path(work_log) %></td>
    <td><%= link_to 'Destroy', work_log, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Work log', new_work_log_path %>
